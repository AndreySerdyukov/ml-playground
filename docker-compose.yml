# Local run of the whole app: backend (FastAPI) + frontend (nginx serving static files).
services:
  backend:
    build: ./backend
    environment:
      APP_CORS_ORIGINS: '["http://localhost:3000"]'
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    depends_on:
      - backend
    ports:
      - "3000:80"
